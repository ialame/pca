name: Second workfow 2
on: [push]
env:
  NIVEAU_WORKFLOW: "Je suis une variable de workflow"

jobs:
  mon_job:
    runs-on: ubuntu-latest
    env:
      NIVEAU_JOB: "Je suis une variable de job"
    steps:
      - name: Étape 1
        env:
          NIVEAU_ETAPE: "Je suis une variable d'étape"
        run: |
          echo "$NIVEAU_WORKFLOW"
          echo "$NIVEAU_JOB"
          echo "$NIVEAU_ETAPE"
          echo VAR_4 $VAR_4
          echo SECRET_1 $SECRET_1
          echo Mon secret est ${{ secrets.SECRET_1 }}
          
